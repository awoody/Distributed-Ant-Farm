package master;

/**
 * Another abstract initializer class, meant to force
 * anyone using this architecture to extend this class
 * in order to gain an instance of the master server and
 * their implementation of the master engine.
 * 
 * @author Alex Woody
 *         CS 587 Fall 2011 - DAF Project Group
 *
 */
public abstract class AbstractMasterInitializer 
{
	/**
	 * Main method should instantiate a master server,
	 * then the implementation of the master engine,
	 * passing the master server as an arg.  Finally,
	 * it should run the master server.
	 * 
	 * @param args
	 */
	public static void main(String [] args)
	{
		
	}
}
